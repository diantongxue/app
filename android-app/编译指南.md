# Android App 编译指南

## 📋 编译前准备

### 方案A：使用 Android Studio（推荐）

1. **安装 Android Studio**
   - 下载：https://developer.android.com/studio
   - 安装并打开 Android Studio
   - 首次启动会自动安装 Android SDK 和 Gradle

2. **打开项目**
   - 打开 Android Studio
   - 选择 "Open an Existing Project"
   - 选择 `android-app` 目录

3. **编译 APK**
   - 点击菜单：`Build` → `Build Bundle(s) / APK(s)` → `Build APK(s)`
   - 或者使用命令行：`./gradlew assembleDebug`

### 方案B：命令行编译（需要先安装工具）

#### 1. 安装 Java JDK
```bash
# 使用 Homebrew 安装
brew install openjdk@17

# 设置环境变量（添加到 ~/.zshrc）
export JAVA_HOME=$(/usr/libexec/java_home -v 17)
export PATH="$JAVA_HOME/bin:$PATH"
```

#### 2. 安装 Android SDK
```bash
# 使用 Homebrew 安装 Android SDK Command Line Tools
brew install --cask android-commandlinetools

# 设置环境变量（添加到 ~/.zshrc）
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools
```

#### 3. 安装 SDK 组件
```bash
# 接受许可证并安装必要的 SDK 组件
sdkmanager --licenses
sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"
```

#### 4. 创建 Gradle Wrapper
```bash
cd android-app
# 如果项目没有 gradlew，需要先安装 gradle
brew install gradle
gradle wrapper
```

#### 5. 编译 APK
```bash
cd android-app
./gradlew assembleDebug
```

编译完成后，APK 文件位于：
```
android-app/app/build/outputs/apk/debug/app-debug.apk
```

## 🚀 快速编译（如果已安装 Android Studio）

如果你已经安装了 Android Studio，可以直接使用：

```bash
cd android-app
./gradlew assembleDebug
```

如果没有 `gradlew` 文件，需要先创建：
```bash
cd android-app
# 使用 Android Studio 的 Gradle
# 或者在 Android Studio 中打开项目，会自动创建
```

## 📱 安装到手机

### 方法1：USB 安装
```bash
# 连接手机后
adb install app/build/outputs/apk/debug/app-debug.apk
```

### 方法2：直接传输
- 将 APK 文件传输到手机
- 在手机上点击安装

## ⚠️ 常见问题

### 问题1：找不到 Java
**解决**：安装 Java JDK 17 或更高版本

### 问题2：找不到 Android SDK
**解决**：安装 Android Studio 或手动安装 Android SDK

### 问题3：Gradle 构建失败
**解决**：
- 检查网络连接（需要下载依赖）
- 检查 `gradle.properties` 配置
- 清理并重新构建：`./gradlew clean build`

### 问题4：签名问题
**解决**：Debug 版本不需要签名，Release 版本需要配置签名

## 📝 注意事项

1. **首次编译**：需要下载大量依赖，可能需要较长时间
2. **网络要求**：需要访问 Google Maven 和 Maven Central
3. **磁盘空间**：确保有足够的磁盘空间（至少 2GB）

## 🔧 当前系统状态检查

运行以下命令检查你的系统：

```bash
# 检查 Java
java -version

# 检查 Android SDK
echo $ANDROID_HOME
ls $ANDROID_HOME

# 检查 Gradle
gradle -v
```

## 💡 推荐方案

**最简单的方式**：安装 Android Studio
- 自动安装所有必要工具
- 提供图形界面
- 支持调试和测试

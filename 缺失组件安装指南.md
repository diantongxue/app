# 📋 缺失组件安装指南

## 📊 当前状态

### ✅ 已安装的组件
1. Java JDK 17 ✅
2. Gradle ✅
3. Android SDK 基础 ✅
4. platform-tools ✅
5. Gradle Wrapper ✅

### ❌ 缺失的组件
1. **Android SDK Command Line Tools** (sdkmanager) ❌
2. **platforms;android-34** ❌
3. **build-tools;34.0.0** ❌

---

## 🚀 安装方法

### 方法1：使用自动安装脚本（推荐）

在 Mac 终端中运行：

```bash
cd /Users/mac/Desktop/故乡食品/app
bash Mac终端安装脚本.sh
```

这个脚本会自动：
1. 下载 Android SDK Command Line Tools
2. 解压并安装
3. 接受许可证
4. 安装缺失的 SDK 组件
5. 验证安装

---

### 方法2：手动执行命令

在 Mac 终端中，按顺序执行以下命令：

```bash
# 1. 进入 Android SDK 目录
cd $HOME/Library/Android/sdk/cmdline-tools

# 2. 清理旧文件
rm -rf commandlinetools.zip cmdline-tools latest

# 3. 下载 Android SDK Command Line Tools
curl -L -o commandlinetools.zip https://dl.google.com/android/repository/commandlinetools-mac-11076708_latest.zip

# 4. 解压并安装
unzip -q commandlinetools.zip
mv cmdline-tools latest
rm -f commandlinetools.zip

# 5. 配置环境变量
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin

# 6. 接受许可证
yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses

# 7. 安装 SDK 组件
$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"

# 8. 验证安装
ls -d $ANDROID_HOME/platform-tools && echo "✅ platform-tools"
ls -d $ANDROID_HOME/platforms/android-34 && echo "✅ platforms;android-34"
ls -d $ANDROID_HOME/build-tools/34.0.0 && echo "✅ build-tools;34.0.0"
```

---

### 方法3：一键复制所有命令

我已经创建了 `终端安装命令.txt` 文件，你可以：

1. 打开文件：`终端安装命令.txt`
2. 复制所有命令
3. 粘贴到 Mac 终端中执行

---

## ⏱️ 预计时间

- **下载 Command Line Tools**: 1-2 分钟（约 9MB）
- **安装 SDK 组件**: 3-5 分钟（取决于网络速度）
- **总时间**: 约 5-10 分钟

---

## ✅ 安装完成后的操作

安装完成后，编译 APK：

```bash
cd /Users/mac/Desktop/故乡食品/app/android-app
./gradlew assembleDebug
```

---

## 🆘 如果遇到问题

### 问题1：下载失败
- 检查网络连接
- 确保代理正常工作
- 可以手动下载：https://dl.google.com/android/repository/commandlinetools-mac-11076708_latest.zip

### 问题2：解压失败
- 检查 ZIP 文件是否完整
- 重新下载

### 问题3：sdkmanager 不可用
- 检查 `$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager` 是否存在
- 确保有执行权限：`chmod +x $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager`

---

## 📝 完整命令列表

所有命令已保存在 `终端安装命令.txt` 文件中，可以直接复制使用。

---

**现在请在 Mac 终端中运行 `bash Mac终端安装脚本.sh` 或复制 `终端安装命令.txt` 中的命令执行！** 🚀

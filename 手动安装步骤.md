# 📋 手动安装步骤（网络问题时的备选方案）

如果自动安装脚本因网络问题失败，请按照以下步骤手动安装。

---

## 🔧 步骤 1：修复 Java 配置

在终端中执行：

```bash
# 设置 Java 路径
export JAVA_HOME="/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home"
export PATH="$JAVA_HOME/bin:$PATH"

# 验证
java -version

# 永久配置
echo '' >> ~/.zshrc
echo '# Java 配置' >> ~/.zshrc
echo 'export JAVA_HOME="/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home"' >> ~/.zshrc
echo 'export PATH="$JAVA_HOME/bin:$PATH"' >> ~/.zshrc

# 使配置生效
source ~/.zshrc
```

---

## 📦 步骤 2：手动下载 Android SDK Command Line Tools

### 方法 A：通过浏览器下载

1. 访问：https://developer.android.com/studio#command-tools
2. 找到 "Command line tools only" 部分
3. 下载 macOS 版本
4. 将下载的 ZIP 文件保存到：`~/Downloads/commandlinetools-mac-11076708_latest.zip`

### 方法 B：使用代理下载

如果有代理，在终端中执行：

```bash
# 设置代理（根据你的代理配置修改）
export http_proxy="http://127.0.0.1:7890"
export https_proxy="http://127.0.0.1:7890"

# 下载
cd ~/Downloads
curl -L -o commandlinetools-mac-11076708_latest.zip https://dl.google.com/android/repository/commandlinetools-mac-11076708_latest.zip
```

### 安装 Command Line Tools

```bash
# 创建目录
mkdir -p ~/Library/Android/sdk/cmdline-tools
cd ~/Library/Android/sdk/cmdline-tools

# 复制下载的文件（或移动）
cp ~/Downloads/commandlinetools-mac-11076708_latest.zip ./

# 解压
unzip -q commandlinetools-mac-11076708_latest.zip

# 重命名
mv cmdline-tools latest

# 清理
rm -f commandlinetools-mac-11076708_latest.zip
```

---

## 🔑 步骤 3：接受许可证

```bash
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin

# 接受许可证
yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses
```

---

## 📥 步骤 4：安装 SDK 组件

### 方法 A：使用 sdkmanager（推荐）

```bash
# 安装组件
$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"
```

如果网络问题持续，可以：

1. **使用代理**：
   ```bash
   export http_proxy="http://127.0.0.1:7890"
   export https_proxy="http://127.0.0.1:7890"
   $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"
   ```

2. **分步安装**：
   ```bash
   # 先安装 platform-tools
   $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platform-tools"
   
   # 再安装 platform
   $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platforms;android-34"
   
   # 最后安装 build-tools
   $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "build-tools;34.0.0"
   ```

### 方法 B：使用 Android Studio（最简单）

1. 下载并安装 Android Studio：https://developer.android.com/studio
2. 打开 Android Studio
3. 进入：**Preferences** > **Appearance & Behavior** > **System Settings** > **Android SDK**
4. 在 **SDK Platforms** 标签页中：
   - 勾选 **Android 14.0 (API 34)**
5. 在 **SDK Tools** 标签页中：
   - 勾选 **Android SDK Build-Tools 34.0.0**
   - 勾选 **Android SDK Platform-Tools**
6. 点击 **Apply** 开始下载安装

---

## ✅ 步骤 5：验证安装

```bash
# 检查 Java
java -version
echo $JAVA_HOME

# 检查 Android SDK
echo $ANDROID_HOME
ls $ANDROID_HOME/platform-tools
ls $ANDROID_HOME/platforms/android-34
ls $ANDROID_HOME/build-tools/34.0.0
```

---

## 🚀 步骤 6：配置环境变量（永久）

```bash
# 添加到 ~/.zshrc
echo '' >> ~/.zshrc
echo '# Android SDK 配置' >> ~/.zshrc
echo 'export ANDROID_HOME=$HOME/Library/Android/sdk' >> ~/.zshrc
echo 'export PATH="$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools:$ANDROID_HOME/cmdline-tools/latest/bin"' >> ~/.zshrc

# 使配置生效
source ~/.zshrc
```

---

## 🧪 步骤 7：测试编译

```bash
cd /Users/mac/Desktop/故乡食品/app/android-app
./gradlew assembleDebug
```

---

## 🆘 常见问题

### 问题 1：SSL 连接错误

**解决方案**：
- 检查代理设置
- 使用 Android Studio 安装（推荐）
- 或使用国内镜像源

### 问题 2：下载速度慢

**解决方案**：
- 使用代理
- 使用 Android Studio（通常更稳定）
- 分时段重试

### 问题 3：Build Tools 34.0.0 找不到

**解决方案**：
- 确认已安装：`ls $ANDROID_HOME/build-tools/34.0.0`
- 如果不存在，使用 Android Studio 安装
- 或手动下载并解压到对应目录

---

## 📝 完整命令列表（一键复制）

```bash
# 1. 配置 Java
export JAVA_HOME="/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home"
export PATH="$JAVA_HOME/bin:$PATH"
echo 'export JAVA_HOME="/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home"' >> ~/.zshrc
echo 'export PATH="$JAVA_HOME/bin:$PATH"' >> ~/.zshrc

# 2. 配置 Android SDK
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin
echo 'export ANDROID_HOME=$HOME/Library/Android/sdk' >> ~/.zshrc
echo 'export PATH="$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools:$ANDROID_HOME/cmdline-tools/latest/bin"' >> ~/.zshrc

# 3. 接受许可证
yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses

# 4. 安装组件
$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"

# 5. 使配置生效
source ~/.zshrc

# 6. 测试编译
cd /Users/mac/Desktop/故乡食品/app/android-app
./gradlew assembleDebug
```

---

**推荐：如果网络问题持续，使用 Android Studio 安装是最简单可靠的方法！** 🎯

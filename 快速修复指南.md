# 🚀 快速修复指南

## ❌ 当前问题

执行 `./gradlew assembleDebug` 时出现错误：
```
Unable to locate a Java Runtime.
```

## ✅ 问题原因

Java 已安装，但 `JAVA_HOME` 环境变量配置错误。`.zshrc` 中使用了 `/usr/libexec/java_home`，但 Java 是通过 Homebrew 安装的，不在系统默认位置。

## 🔧 解决方案

### 方法1：运行完整安装脚本（推荐）

在 Mac 终端中运行：

```bash
cd /Users/mac/Desktop/故乡食品/app
bash 完整安装和配置.sh
```

这个脚本会：
1. ✅ 修复 Java 配置
2. ✅ 配置 Android SDK 环境变量
3. ✅ 安装缺失的 Android SDK 组件
4. ✅ 验证所有安装

---

### 方法2：只修复 Java 配置

如果只需要修复 Java 配置：

```bash
cd /Users/mac/Desktop/故乡食品/app
bash 修复Java配置.sh
source ~/.zshrc
```

---

### 方法3：手动修复

在 Mac 终端中执行以下命令：

```bash
# 1. 设置 Java 路径
export JAVA_HOME="/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home"
export PATH="$JAVA_HOME/bin:$PATH"

# 2. 验证 Java
java -version

# 3. 永久配置（添加到 ~/.zshrc）
echo '' >> ~/.zshrc
echo '# Java 配置' >> ~/.zshrc
echo 'export JAVA_HOME="/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home"' >> ~/.zshrc
echo 'export PATH="$JAVA_HOME/bin:$PATH"' >> ~/.zshrc

# 4. 使配置生效
source ~/.zshrc

# 5. 测试编译
cd /Users/mac/Desktop/故乡食品/app/android-app
./gradlew assembleDebug
```

---

## 📋 安装脚本说明

### 1. `完整安装和配置.sh` ⭐ 推荐
- 修复 Java 配置
- 配置 Android SDK
- 安装所有缺失组件
- 一键完成所有配置

### 2. `修复Java配置.sh`
- 只修复 Java 环境变量
- 快速解决 Java 问题

### 3. `Mac终端安装脚本.sh`
- 只安装 Android SDK 组件
- 不包含 Java 配置修复

---

## ⚠️ 重要提示

运行脚本后，**必须**执行以下命令使环境变量生效：

```bash
source ~/.zshrc
```

或者**重新打开终端窗口**。

---

## 🧪 验证安装

运行以下命令验证：

```bash
# 检查 Java
java -version
echo $JAVA_HOME

# 检查 Android SDK
echo $ANDROID_HOME
ls $ANDROID_HOME/platforms/android-34
ls $ANDROID_HOME/build-tools/34.0.0

# 测试编译
cd /Users/mac/Desktop/故乡食品/app/android-app
./gradlew assembleDebug
```

---

## 🆘 如果还有问题

1. **检查 Java 安装路径**：
   ```bash
   ls -la /opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home
   ```

2. **检查环境变量**：
   ```bash
   echo $JAVA_HOME
   echo $ANDROID_HOME
   ```

3. **重新加载配置**：
   ```bash
   source ~/.zshrc
   ```

---

**现在请运行 `bash 完整安装和配置.sh` 来修复所有问题！** 🚀
